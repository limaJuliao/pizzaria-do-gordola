export default class Produto {
  constructor(produtoId, descricao, precoUnitario, categoriaId) {
    this.produtoId = produtoId;
    this.descricao = descricao;
    this.precoUnitario = precoUnitario;
    this.categoriaId = categoriaId;
  }
}
